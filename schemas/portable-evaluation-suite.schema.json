{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/JackieL233/fengshui-master/schemas/portable-evaluation-suite.schema.json",
  "title": "FengShui Master Portable Evaluation Suite",
  "type": "object",
  "required": ["name", "version", "purpose", "grading_note", "cases"],
  "properties": {
    "name": {
      "type": "string",
      "const": "fengshui-master-portable-evaluation-suite"
    },
    "version": { "type": "string" },
    "purpose": { "type": "string" },
    "grading_note": { "type": "string" },
    "cases": {
      "type": "array",
      "minItems": 5,
      "items": { "$ref": "#/$defs/evaluationCase" }
    }
  },
  "additionalProperties": false,
  "$defs": {
    "evaluationCase": {
      "type": "object",
      "required": [
        "id",
        "domain",
        "prompt",
        "expected_references",
        "must_include",
        "must_not_include",
        "boundary_focus"
      ],
      "properties": {
        "id": { "type": "string" },
        "domain": { "type": "string" },
        "prompt": { "type": "string" },
        "expected_references": { "$ref": "#/$defs/stringList" },
        "must_include": { "$ref": "#/$defs/stringList" },
        "must_not_include": { "$ref": "#/$defs/stringList" },
        "boundary_focus": { "type": "string" }
      },
      "additionalProperties": false
    },
    "stringList": {
      "type": "array",
      "items": { "type": "string" },
      "minItems": 1,
      "uniqueItems": true
    }
  }
}
